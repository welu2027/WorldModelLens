# Core API

Core abstractions live under `world_model_lens.core`.

## Key modules

- `world_model_lens.core.config`
- `world_model_lens.core.world_state`
- `world_model_lens.core.world_trajectory`
- `world_model_lens.core.hooks`
- `world_model_lens.hooked_world_model`

## Notes

This is a stable starter page for the docs scaffold.

When you want richer API output, replace these module lists with `automodule` blocks after docstring cleanup.
