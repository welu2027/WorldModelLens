[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short -m "not network"
pythonpath = .
markers =
    network: marks tests that require network access and HuggingFace downloads (deselect with '-m "not network"')
